# Copy this file to .env and fill in your values.
# Generate JWT_SECRET with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"

JWT_SECRET=replace_with_a_long_random_hex_string
PORT=3001
NODE_ENV=development
# CLIENT_ORIGIN=https://your-deployed-app.example.com  # set in production
